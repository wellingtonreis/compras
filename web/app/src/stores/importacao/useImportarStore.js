import { defineStore } from 'pinia';
export const useImportarStore = defineStore('useImportarStore', {
  state: () => ({
    
  }),
  actions: {
    submitForm() {
    },
    resetForm() {
    }
  }
});
